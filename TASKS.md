# TASKS

このファイルは、AI エージェントへ実装指示を渡すためのタスク一覧です。

## 運用ルール

- タスクはチェックボックス形式で記述する
- 1 行目のタスクは、できるだけ機能要求を短く表す
- まだ着手しない検討中の論点は `NOTES.md` に書いてよい
- 今回スコープ外で見送る改善提案は `PROPOSALS.md` に残す
- 実装開始前に、対象タスクを詳細タスクへ分解する
- 詳細タスクは、このファイル内へ追記して管理する
- 実装は TDD で進める
- TDD の流れは `テスト実装 → テスト Red 確認 → 実装 → テスト Green 確認 → リファクタリング` を守る
- 実装前に `make check` を実行し、現状の問題有無を確認する
- 先にテストを実装し、失敗を確認してから実装する
- テストが Green になったあとにリファクタリングを行う
- リファクタリング後はテストを再確認する
- 実装後に再度 `make check` を実行し、問題がないことを確認する
- 完了したタスクは `- [x]` に更新する
- 一時的なメモではなく、完了状態が分かる粒度で残す
- 実装中に見つけた改善提案をタスク化しない場合は `PROPOSALS.md` へ記録する

## 検討中

- [ ] サンプル検討: 課金転換戦略変更の前提条件を整理する
  - メモは [NOTES.md](./NOTES.md) に残す
  - 実装着手が決まったら未着手タスクへ移す

## 未着手タスク

- [x] Next.js プロジェクトを作成する
  - [x] 既存構成と影響範囲を確認する
  - [x] 必要な詳細タスクへ分解する
  - [x] `make check` を実行して現状を確認する
  - [x] Next.js の初期構成と実行コマンドを定義する
  - [x] 検証用テスト・チェックコマンドを整備する
  - [x] 対象コマンドが成功することを確認する
  - [x] 必要なら `docs` を更新する
  - [x] `AGENTS.md` に今回の学びを記録する
- [x] トップページから複数ゲームを選べる構成に整理する
  - [x] 現在のゲーム配置とルーティングを確認する
  - [x] 必要な詳細タスクへ分解する
  - [x] `make check` を実行して現状を確認する
  - [x] 新構成向けのテストを実装する
  - [x] 対象テストが Red になることを確認する
  - [x] トップページとゲーム別ルートを実装する
  - [x] `src/features/games` を起点にした登録構成を追加する
  - [x] 対象テストが Green になることを確認する
  - [x] `docs` を更新する
  - [x] `AGENTS.md` に今回の学びを記録する
  - [x] `make check` を再実行して成功を確認する
- [ ] サンプルタスク: 課金転換戦略をデータロック（30日制限）に変更する
  - [ ] 対象仕様と影響範囲を確認する
  - [ ] 必要な詳細タスクへ分解する
  - [ ] `make check` を実行して現状を確認する
  - [ ] テストを実装する
  - [ ] 対象テストが Red になることを確認する
  - [ ] 実装を行う
  - [ ] 対象テストが Green になることを確認する
  - [ ] リファクタリングを行う
  - [ ] リファクタリング後のテストを確認する
  - [ ] `make check` を再実行して成功を確認する
  - [ ] 必要なら `docs` を更新する

## 詳細タスク分解テンプレート

- [ ] 親タスク名
  - [ ] 仕様確認
  - [ ] 影響範囲確認
  - [ ] テスト観点洗い出し
  - [ ] `make check` 実行
  - [ ] テスト実装
  - [ ] 対象テスト Red 確認
  - [ ] 実装
  - [ ] 対象テスト Green 確認
  - [ ] リファクタリング
  - [ ] リファクタリング後テスト確認
  - [ ] `make check` 再実行
  - [ ] `docs` 更新要否確認

## タスク化前メモの流れ

1. 検討段階の論点や要確認事項を [NOTES.md](./NOTES.md) に書く
2. 実装することが決まったら、このファイルの `検討中` へ短く追加する
3. 着手時に詳細タスクへ分解して `未着手タスク` へ移す
